{
  "version": 1,
  "runId": "9eb7b7ef-afab-4bd0-b7e3-d7788220dadf",
  "timestamp": "2026-08-31T16:18:09.253Z",
  "targetProject": "/home/olumide/Desktop/webmcp-coffee-store",
  "sourceVersion": "985c3afcbe8fd306f0614f9cc7c6c292b36596a1",
  "workingTreeHash": "2427d211a9ae0329ec41bd120141f6ed9420c188a8ddacf96ab82724dc37b1a2",
  "patchPath": "/home/olumide/Desktop/webmcp-coffee-store/.webmcpify/pending-diff.patch",
  "generationTrajectory": "/home/olumide/Desktop/WebMCPify/trajectories/generate-2026-08-31T16-16-35-573Z-9b3d6aa5.json",
  "changedFiles": [
    "src/components/CoffeeRoastFilter.tsx",
    "src/App.tsx"
  ],
  "patchStatus": "awaiting-review",
  "patch": "diff --git a/src/components/CoffeeRoastFilter.tsx b/src/components/CoffeeRoastFilter.tsx\n--- a/src/components/CoffeeRoastFilter.tsx\n+++ b/src/components/CoffeeRoastFilter.tsx\n@@ -28,7 +28,12 @@ export function CoffeeRoastFilter({ onFilter }: CoffeeRoastFilterProps) {\n   }\n \n   return (\n-    <form className=\"roast-filter-form\" onSubmit={handleSubmit}>\n+    <form\n+      className=\"roast-filter-form\"\n+      onSubmit={handleSubmit}\n+      toolname=\"filter_by_roast\"\n+      tooldescription=\"Filter the visible coffee catalog by roast level\"\n+    >\n       <label htmlFor=\"roast-filter\">Roast</label>\n       <select\n         defaultValue=\"all\"\n@@ -37,6 +41,7 @@ export function CoffeeRoastFilter({ onFilter }: CoffeeRoastFilterProps) {\n         name=\"roast\"\n         onChange={handleChange}\n         required\n+        toolparam-description=\"Roast level to filter by (all, light, medium, dark)\"\n       >\n         <option value=\"all\">All</option>\n         <option value=\"light\">Light</option>\ndiff --git a/src/App.tsx b/src/App.tsx\n--- a/src/App.tsx\n+++ b/src/App.tsx\n@@ -1,4 +1,4 @@\n-import { useState } from 'react'\n+import { useEffect, useState } from 'react'\n import './App.css'\n import { CartPanel } from './components/CartPanel'\n import { ProductCard } from './components/ProductCard'\n@@ -7,6 +7,7 @@ import { ComparisonTray } from './components/ComparisonTray'\n import { TastingFlightPanel } from './components/TastingFlightPanel'\n import { products, type ProductId } from './data/products'\n import { useShopStore } from './store/useShopStore'\n+import { isProductId } from './data/products'\n import { CoffeeRoastFilter, type RoastFilter } from './components/CoffeeRoastFilter'\n \n function App() {\n@@ -37,6 +38,143 @@ function App() {\n     })\n   }\n \n+  useEffect(() => {\n+    if (typeof navigator === 'undefined' || !('modelContext' in navigator)) {\n+      return\n+    }\n+\n+    const modelContext = (navigator as unknown as { modelContext: { registerTool: (tool: unknown) => () => void } }).modelContext\n+    const unregisterCallbacks: (() => void)[] = []\n+\n+    unregisterCallbacks.push(\n+      modelContext.registerTool({\n+        name: 'add_to_cart',\n+        description: 'Add a coffee to the shopping cart by product ID with an optional quantity (1 to 99).',\n+        parameters: {\n+          type: 'object',\n+          properties: {\n+            productId: {\n+              type: 'string',\n+              enum: ['ethiopia-guji', 'colombia-huila', 'kenya-nyeri', 'brazil-cerrado', 'guatemala-huehue', 'sumatra-gayo'],\n+              description: 'The unique product ID of the coffee to add.',\n+            },\n+            quantity: {\n+              type: 'integer',\n+              minimum: 1,\n+              maximum: 99,\n+              description: 'The number of bags to add (defaults to 1).',\n+            },\n+          },\n+          required: ['productId'],\n+        },\n+        handler: async ({ productId, quantity = 1 }: { productId: string; quantity?: number }) => {\n+          if (!isProductId(productId)) throw new Error(`Invalid productId: ${productId}`)\n+          useShopStore.getState().addToCart(productId, quantity)\n+          return { success: true, cart: useShopStore.getState().cart }\n+        },\n+      }),\n+    )\n+\n+    unregisterCallbacks.push(\n+      modelContext.registerTool({\n+        name: 'update_cart_quantity',\n+        description: 'Update the quantity of a specific coffee product currently in the shopping cart.',\n+        parameters: {\n+          type: 'object',\n+          properties: {\n+            productId: {\n+              type: 'string',\n+              enum: ['ethiopia-guji', 'colombia-huila', 'kenya-nyeri', 'brazil-cerrado', 'guatemala-huehue', 'sumatra-gayo'],\n+              description: 'The unique product ID in the cart.',\n+            },\n+            quantity: {\n+              type: 'integer',\n+              minimum: 1,\n+              maximum: 99,\n+              description: 'The new quantity (1 to 99).',\n+            },\n+          },\n+          required: ['productId', 'quantity'],\n+        },\n+        handler: async ({ productId, quantity }: { productId: string; quantity: number }) => {\n+          if (!isProductId(productId)) throw new Error(`Invalid productId: ${productId}`)\n+          useShopStore.getState().updateCartQuantity(productId, quantity)\n+          return { success: true, cart: useShopStore.getState().cart }\n+        },\n+      }),\n+    )\n+\n+    unregisterCallbacks.push(\n+      modelContext.registerTool({\n+        name: 'remove_from_cart',\n+        description: 'Remove a coffee product entirely from the shopping cart.',\n+        parameters: {\n+          type: 'object',\n+          properties: {\n+            productId: {\n+              type: 'string',\n+              enum: ['ethiopia-guji', 'colombia-huila', 'kenya-nyeri', 'brazil-cerrado', 'guatemala-huehue', 'sumatra-gayo'],\n+              description: 'The unique product ID to remove.',\n+            },\n+          },\n+          required: ['productId'],\n+        },\n+        handler: async ({ productId }: { productId: string }) => {\n+          if (!isProductId(productId)) throw new Error(`Invalid productId: ${productId}`)\n+          useShopStore.getState().removeFromCart(productId)\n+          return { success: true, cart: useShopStore.getState().cart }\n+        },\n+      }),\n+    )\n+\n+    unregisterCallbacks.push(\n+      modelContext.registerTool({\n+        name: 'set_auth_state',\n+        description: 'Set the demo authentication state (log in or log out of the coffee store).',\n+        parameters: {\n+          type: 'object',\n+          properties: {\n+            isLoggedIn: {\n+              type: 'boolean',\n+              description: 'Set to true to log in, false to log out.',\n+            },\n+          },\n+          required: ['isLoggedIn'],\n+        },\n+        handler: async ({ isLoggedIn }: { isLoggedIn: boolean }) => {\n+          if (isLoggedIn) {\n+            useShopStore.getState().login()\n+          } else {\n+            useShopStore.getState().logout()\n+          }\n+          return { success: true, isLoggedIn: useShopStore.getState().isLoggedIn }\n+        },\n+      }),\n+    )\n+\n+    if (isLoggedIn) {\n+      unregisterCallbacks.push(\n+        modelContext.registerTool({\n+          name: 'checkout',\n+          description: 'Complete the checkout process for all items currently in the cart. Requires user to be logged in.',\n+          parameters: {\n+            type: 'object',\n+            properties: {},\n+            required: [],\n+          },\n+          handler: async () => {\n+            useShopStore.getState().checkout()\n+            return { success: true, notice: useShopStore.getState().notice }\n+          },\n+        }),\n+      )\n+    }\n+\n+    return () => {\n+      unregisterCallbacks.forEach((unregister) => unregister?.())\n+    }\n+  }, [isLoggedIn])\n+\n   return (\n     <div className=\"app\">\n       <main className=\"catalog\" aria-label=\"Coffee collection\">\n"
}
