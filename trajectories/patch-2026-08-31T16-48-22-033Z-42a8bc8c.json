{
  "version": 1,
  "runId": "0f51246b-7502-4740-abaa-ad9e4a2deda3",
  "timestamp": "2026-08-31T16:48:21.921Z",
  "targetProject": "/home/olumide/Desktop/webmcp-coffee-store",
  "sourceVersion": "985c3afcbe8fd306f0614f9cc7c6c292b36596a1",
  "workingTreeHash": "17bef1469443ab93f7465fed2a7ad0bf156d941029d3655c19e78246e21315a2",
  "patchPath": "/home/olumide/Desktop/webmcp-coffee-store/.webmcpify/pending-diff.patch",
  "generationTrajectory": "/home/olumide/Desktop/WebMCPify/trajectories/generate-2026-08-31T16-46-15-783Z-bfd61bc1.json",
  "changedFiles": [
    "src/components/CoffeeRoastFilter.tsx",
    "src/App.tsx"
  ],
  "patchStatus": "awaiting-review",
  "patch": "diff --git a/src/components/CoffeeRoastFilter.tsx b/src/components/CoffeeRoastFilter.tsx\n--- a/src/components/CoffeeRoastFilter.tsx\n+++ b/src/components/CoffeeRoastFilter.tsx\n@@ -30,5 +30,10 @@ export function CoffeeRoastFilter({ onFilter }: CoffeeRoastFilterProps) {\n   return (\n-    <form className=\"roast-filter-form\" onSubmit={handleSubmit}>\n+    <form\n+      className=\"roast-filter-form\"\n+      onSubmit={handleSubmit}\n+      toolname=\"filter_roast\"\n+      tooldescription=\"Filter the displayed coffee catalog by roast level (all, light, medium, or dark).\"\n+    >\n       <label htmlFor=\"roast-filter\">Roast</label>\n       <select\n         defaultValue=\"all\"\ndiff --git a/src/App.tsx b/src/App.tsx\n--- a/src/App.tsx\n+++ b/src/App.tsx\n@@ -1,4 +1,4 @@\n-import { useState } from 'react'\n+import { useEffect, useState } from 'react'\n import './App.css'\n import { CartPanel } from './components/CartPanel'\n import { ProductCard } from './components/ProductCard'\n@@ -20,6 +20,138 @@ function App() {\n   const login = useShopStore((state) => state.login)\n   const logout = useShopStore((state) => state.logout)\n \n+  useEffect(() => {\n+    const modelContext = (navigator as unknown as {\n+      modelContext?: {\n+        registerTool: (tool: unknown) => (() => void) | void\n+      }\n+    }).modelContext\n+    if (!modelContext?.registerTool) return\n+\n+    const unregisterAddToCart = modelContext.registerTool({\n+      name: 'add_to_cart',\n+      description: 'Add a coffee product to the shopping cart with a specified quantity.',\n+      parameters: {\n+        type: 'object',\n+        properties: {\n+          productId: {\n+            type: 'string',\n+            enum: ['ethiopia-guji', 'colombia-huila', 'kenya-nyeri', 'brazil-cerrado', 'guatemala-huehue', 'sumatra-gayo'],\n+            description: 'The unique product ID of the coffee to add',\n+          },\n+          quantity: {\n+            type: 'integer',\n+            minimum: 1,\n+            maximum: 99,\n+            description: 'The number of bags to add (default is 1)',\n+          },\n+        },\n+        required: ['productId'],\n+      },\n+      execute: async ({ productId, quantity }: { productId: ProductId; quantity?: number }) => {\n+        useShopStore.getState().addToCart(productId, quantity)\n+        return { success: true, cart: useShopStore.getState().cart }\n+      },\n+    })\n+\n+    const unregisterRemoveFromCart = modelContext.registerTool({\n+      name: 'remove_from_cart',\n+      description: 'Remove a coffee product completely from the shopping cart by product ID.',\n+      parameters: {\n+        type: 'object',\n+        properties: {\n+          productId: {\n+            type: 'string',\n+            enum: ['ethiopia-guji', 'colombia-huila', 'kenya-nyeri', 'brazil-cerrado', 'guatemala-huehue', 'sumatra-gayo'],\n+            description: 'The unique product ID of the coffee to remove',\n+          },\n+        },\n+        required: ['productId'],\n+      },\n+      execute: async ({ productId }: { productId: ProductId }) => {\n+        useShopStore.getState().removeFromCart(productId)\n+        return { success: true, cart: useShopStore.getState().cart }\n+      },\n+    })\n+\n+    const unregisterUpdateCartQuantity = modelContext.registerTool({\n+      name: 'update_cart_quantity',\n+      description: 'Update the quantity of a specific coffee product currently in the cart.',\n+      parameters: {\n+        type: 'object',\n+        properties: {\n+          productId: {\n+            type: 'string',\n+            enum: ['ethiopia-guji', 'colombia-huila', 'kenya-nyeri', 'brazil-cerrado', 'guatemala-huehue', 'sumatra-gayo'],\n+            description: 'The unique product ID of the coffee to update',\n+          },\n+          quantity: {\n+            type: 'integer',\n+            minimum: 1,\n+            maximum: 99,\n+            description: 'The new quantity (1-99)',\n+          },\n+        },\n+        required: ['productId', 'quantity'],\n+      },\n+      execute: async ({ productId, quantity }: { productId: ProductId; quantity: number }) => {\n+        useShopStore.getState().updateCartQuantity(productId, quantity)\n+        return { success: true, cart: useShopStore.getState().cart }\n+      },\n+    })\n+\n+    const unregisterLogin = modelContext.registerTool({\n+      name: 'login',\n+      description: 'Log in to the demo user account, enabling the checkout tool.',\n+      parameters: { type: 'object', properties: {} },\n+      execute: async () => {\n+        useShopStore.getState().login()\n+        return { success: true, isLoggedIn: true }\n+      },\n+    })\n+\n+    const unregisterLogout = modelContext.registerTool({\n+      name: 'logout',\n+      description: 'Log out of the demo user account, revoking checkout availability.',\n+      parameters: { type: 'object', properties: {} },\n+      execute: async () => {\n+        useShopStore.getState().logout()\n+        return { success: true, isLoggedIn: false }\n+      },\n+    })\n+\n+    return () => {\n+      if (typeof unregisterAddToCart === 'function') unregisterAddToCart()\n+      if (typeof unregisterRemoveFromCart === 'function') unregisterRemoveFromCart()\n+      if (typeof unregisterUpdateCartQuantity === 'function') unregisterUpdateCartQuantity()\n+      if (typeof unregisterLogin === 'function') unregisterLogin()\n+      if (typeof unregisterLogout === 'function') unregisterLogout()\n+    }\n+  }, [])\n+\n+  useEffect(() => {\n+    const modelContext = (navigator as unknown as {\n+      modelContext?: {\n+        registerTool: (tool: unknown) => (() => void) | void\n+      }\n+    }).modelContext\n+    if (!modelContext?.registerTool || !isLoggedIn) return\n+\n+    const unregisterCheckout = modelContext.registerTool({\n+      name: 'checkout',\n+      description: 'Complete the checkout process for all items in the cart (conditionally registered only when logged in).',\n+      parameters: { type: 'object', properties: {} },\n+      execute: async () => {\n+        useShopStore.getState().checkout()\n+        return { success: true, notice: useShopStore.getState().notice }\n+      },\n+    })\n+\n+    return () => {\n+      if (typeof unregisterCheckout === 'function') unregisterCheckout()\n+    }\n+  }, [isLoggedIn])\n+\n   const visibleProducts = products.filter(\n     (product) =>\n       roastFilter === 'all' || product.roast.toLowerCase() === roastFilter,\n"
}
